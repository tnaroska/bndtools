/*
 * This file is NOT maintained automatically by Bndtools.
 *
 * Build customisations that are applied to the root project.
 */

assert(project == rootProject)


distclean.doLast {
  file("$bnd_cnf/releaserepo").listFiles().each {
    if (it.name != ".gitignore") {
      delete(it)
    }
  }
}
